function get_answer() {
  // look for local cache, if not found, fetch from remote server
}

// 一共五个字, 返回七种状态

// 提示: 数字(笔画), 部首

// 全部正确: 111
// 部首正确: 001
// 笔画正确: 010
// 位置正确: 100 (实际只能是 110, 101)
//
function guess_status(answer: string, curr: string): number {
  return 0;
}

window.onload = () => {
  //
};
